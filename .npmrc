# Consumo de paquetes del monorepo @app-core-ars
# Produccion/CI: se resuelven desde GitHub Packages (cuando esten publicados).
# Desarrollo local (esta maquina): override via file: en package.json.
@app-core-ars:registry=https://npm.pkg.github.com
# Auth para publicar/leer paquetes privados (NO commitear el token).
#//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
